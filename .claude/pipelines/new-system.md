# 새 시스템 생성 파이프라인

새로운 Unity 시스템을 생성할 때 이 파이프라인을 따릅니다.

## 사용법

```
"Inventory 시스템 만들어줘"
→ 이 파이프라인 참고해서 처리
```

## 입력 파라미터

- `{{system_name}}`: 시스템 이름 (예: Inventory, Combat, Quest)
- `{{requirements}}`: 기능 요구사항 (선택)

---

## 파이프라인 단계

### 1단계: 구조 생성 (스크립트) ⚡

**목적**: 폴더와 빈 파일 생성 (컨텍스트 절약)

```bash
.claude/scripts/create-structure.sh {{system_name}}
```

**결과물**:
- `Assets/01_Scripts/03_Systems/XX_{{system_name}}/`
  - `01_Interfaces/I{{system_name}}.cs`
  - `02_Core/{{system_name}}Controller.cs`
  - `03_Mono/`
- `Assets/01_Scripts/05_Tests/{{system_name}}/`
  - `{{system_name}}ControllerTests.cs`

**컨텍스트 사용**: 최소 (스크립트 실행 결과만)

---

### 2단계: 설계 (architect 에이전트) 🏗️

**목적**: 인터페이스 설계, OCP 확장 포인트 정의

**호출**:
```
architect 에이전트에게:
- system_name: {{system_name}}
- requirements: {{requirements}}
- interface_file: (1단계에서 생성된 경로)
```

**결과물**:
- 완성된 `I{{system_name}}.cs`
- 확장 인터페이스 (필요시)
- 설계 의도 문서

**컨텍스트 사용**: 독립 (서브에이전트)

---

### 3단계: 구현 (implementer 에이전트) 🔨

**목적**: Pure C# 로직 + 테스트 코드 작성

**호출**:
```
implementer 에이전트에게:
- interface_file: 2단계 결과
- controller_file: (1단계에서 생성된 경로)
- test_file: (1단계에서 생성된 경로)
- design_notes: 2단계의 설계 의도
```

**결과물**:
- 완성된 `{{system_name}}Controller.cs`
- 완성된 테스트 파일

**컨텍스트 사용**: 독립 (서브에이전트)

---

### 4단계: 검토 (reviewer 에이전트) ✅

**목적**: SOLID 검토, 안전성 평가

**호출**:
```
reviewer 에이전트에게:
- files: 2단계 + 3단계 결과물
- project_stage: PROJECT_CONTEXT.md에서 읽음
```

**결과물**:
- 리뷰 리포트
- 개선 제안 (있다면)

**컨텍스트 사용**: 독립 (서브에이전트)

---

## 병렬 처리 가능 구간

```
1단계 (스크립트)
    ↓
2단계 (architect)
    ↓
3단계 (implementer)  ← 2단계 완료 후
    ↓
4단계 (reviewer)     ← 3단계 완료 후
```

현재 구조에서는 **순차 실행**이 안전합니다.
(각 단계가 이전 단계 결과에 의존)

---

## 프로젝트 단계별 분기

### Prototype
```
1단계 → 3단계 (설계 생략, 빠른 구현)
```

### Alpha
```
1단계 → 2단계 → 3단계 → 4단계 (전체 파이프라인)
```

### Beta / Live
```
⚠️ 경고: 새 시스템 생성보다 기존 확장 포인트 활용 권장
```

---

## 예상 효과

| 구분 | 기존 방식 | 파이프라인 적용 |
|-----|----------|---------------|
| 컨텍스트 | 100% | ~20% |
| 일관성 | 낮음 | 높음 |
| 폴더 규칙 | 수동 확인 | 자동 적용 |
| SOLID 검토 | 생략 가능 | 필수 포함 |

---

## 사용 예시

```
사용자: "인벤토리 시스템 만들어줘. 아이템 추가/제거/조회 기능 필요해."

→ 메인 에이전트가 이 파이프라인 읽음
→ 1단계: create-structure.sh Inventory 실행
→ 2단계: architect 호출 (인터페이스 설계)
→ 3단계: implementer 호출 (구현 + 테스트)
→ 4단계: reviewer 호출 (검토)
→ 결과 요약 보고
```
